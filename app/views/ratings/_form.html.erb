<%= form_with(model: [recipe, rating], local: true) do |form| %>

  <%= form.label :rating, "Your Rating" %>
  <%= form.number_field :score, min: 1, max: 5 %> / 5

  <div class="actions">
    <%= form.submit nil, class: "btn btn-primary" %>
  </div>
<% end %>
<br>
