<%= link_to category do %>
  <h3 class="recipe-link"><%= category.name %></h3>
<% end %>

<section>
  <div class="category-container">
    <div class="pic-row">  
      <% category.recipes.first(10).each do |recipe| %>
        <% if recipe.image.attached? %>
          <%= link_to recipe do %>
            <%= image_tag recipe.image.variant(resize_to_limit: [500, 500]), class: "rp" %>
          <% end %>
        <% end %>
      <% end %>  
    </div>
  </div>
</section>​ 
